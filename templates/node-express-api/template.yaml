id: node-express-api
name: Node.js Express API
description: A minimal Express REST API (Node.js).
requires:
  - nodejs
recipe:
  - label: Write project files
    workdir: "{{hostStaging}}"
    script: write-project-files.sh
  - label: Install dependencies
    workdir: "{{hostStaging}}/app"
    run: npm install
  - label: Copy source into project
    script: copy-source-into-project.sh
  - label: Configure Build & Run
    script: configure-build-run.sh